model:
  id: mistralai/Mistral-7B-Instruct-v0.3

generation:
  max_new_tokens: 256
  temperature: 0.7
  top_p: 0.9
  do_sample: true

eval:
  tasks:
    - basic_capability

seed: 42
output_dir: results/baseline
